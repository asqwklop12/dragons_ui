version: '3.8'

services:
  app:
    container_name: dragons-ui
    # Build directly on the server (No ECR)
    build:
      context: .
      dockerfile: Dockerfile
      args:
        NEXT_PUBLIC_TOSS_CLIENT_KEY: ${NEXT_PUBLIC_TOSS_CLIENT_KEY}
    ports:
      # External 3000 -> Internal 3001 (Because Dockerfile has EXPOSE 3001)
      - "3000:3001"
    environment:
      - NODE_ENV=production
      - SERVER_API_URL=${SERVER_API_URL}
      # Tell Next.js to start on 3001 internally
      - PORT=3001
    restart: always
    # 네트워크 설정 단순화 (Nginx와 연결 필요 시 해당 네트워크 이름 일치시켜야 함)
    networks:
      - dragons_net

networks:
  dragons_net:
    driver: bridge
    # 외부 네트워크 의존성 제거 (자동 생성)
